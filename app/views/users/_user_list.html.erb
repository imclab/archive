<ul class="user-list">
<% users.each do |u| %>
  <li>
  <%= "#{u.name} (#{u.email})" %> 
  <span class="delete">
    <%= link_to 'Delete this user', u, :method => :delete,
                                       :confirm => "Are you sure?" %>
  </span>
  </li>
<% end %>
</ul>
